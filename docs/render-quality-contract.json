{
  "schema": "lumen-render-quality-v1",
  "version": 1,
  "preconditions": [
    {
      "id": "viewport-geometry",
      "source": "lib/canvas_surface.dart#CanvasViewport.frame",
      "condition": "logical canvas is fitted by min(viewport width/760, viewport height/700), clamped to 0.5..1.0, and centered"
    },
    {
      "id": "tap-inverse",
      "source": "lib/canvas_surface.dart#CanvasViewport.logicalTap",
      "condition": "input uses the same frame transform in reverse and clamps to logical bounds"
    },
    {
      "id": "single-render-path",
      "source": "lib/main.dart#_Game/Scene",
      "condition": "runtime rendering and input delegate viewport math to CanvasViewport"
    }
  ],
  "proofs": [
    {
      "id": "viewport-mapping",
      "command": "flutter test test/canvas_surface_test.dart",
      "evidence": "test/canvas_surface_test.dart#CanvasViewport maps centered taps deterministically"
    },
    {
      "id": "visual-regression",
      "command": "flutter test",
      "evidence": "test/golden_test.dart + test/goldens/*.png"
    },
    {
      "id": "static-analysis",
      "command": "flutter analyze",
      "evidence": "analyzer exit code 0"
    }
  ],
  "decision": {
    "owner": "Lumen Render Quality Gate",
    "mode": "system-adjudicated",
    "humanApprovalRequired": false,
    "failureMode": "fail-closed",
    "progression": "preconditions -> implementation -> deterministic proofs",
    "onlyBlockingCondition": "failed or missing deterministic proof"
  }
}
